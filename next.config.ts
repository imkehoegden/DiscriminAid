import type { NextConfig } from "next";

// sonst können bilder nicht angezeigt werden

const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "cdn.sanity.io",
      },
    ],
  },
};

export default nextConfig;
